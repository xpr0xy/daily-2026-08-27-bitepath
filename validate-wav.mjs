import fs from 'node:fs';const p=process.argv[2];if(!p)throw Error('usage: node validate-wav.mjs file.wav');const b=fs.readFileSync(p);if(b.toString('ascii',0,4)!=='RIFF'||b.toString('ascii',8,12)!=='WAVE')throw Error('not RIFF/WAVE');if(b.readUInt16LE(20)!==1||b.readUInt16LE(22)!==2||b.readUInt32LE(24)!==48000||b.readUInt16LE(34)!==16)throw Error('expected stereo 48k 16-bit PCM');if(b.readUInt32LE(40)!==b.length-44)throw Error('bad data length');console.log(`PASS stereo 48000Hz 16-bit PCM ${b.length} bytes`);
